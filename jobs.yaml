projects:
  rakusta:
    path: https://github.com/melezhik/rakusta.git
    #action: docker build stop
    #action: hello
    action: build
    title: linux builds
    hosts:
      -
        host: https://sparrowhub.io:4000
        queue-id: rakudo-build
        title: alpine build
        action: alpine-setup build
        sparrowdo:
          docker: alpine
          bootstrap: true
          no_sudo: true
      -
        host: https://sparrowhub.io:4000
        queue-id: rakudo-build
        title: debian build
        sparrowdo:
          docker: debian
          bootstrap: true
          no_sudo: true
      -
        host: https://sparrowhub.io:4000
        queue-id: rakudo-build
        title: archlinux build
        sparrowdo:
          docker: archlinux
          bootstrap: true
          no_sudo: true
