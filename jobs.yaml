projects:
  rakusta:
    path: https://github.com/melezhik/rakusta.git
    #action: docker build stop
    #action: hello
    action: build
    sparrowdo:
      docker: alpine
      bootstrap: true
      no_sudo: true
    after:
    -
      name: build
      sparrowdo:
        docker: debian
        bootstrap: true
        no_sudo: true
